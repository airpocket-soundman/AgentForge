# AgentForge — 開発セッション必読

このリポジトリを開発する **すべての LLM セッション（=このプロジェクトでいう「ワーカー」）は、作業を始める前に下記の正式ドキュメントを必ず読むこと。**

## 必読：ワーカー定義と運用ルール（正本）

- **[VISION.md](VISION.md)** — このアプリの**到達点**（目的・柱・安全装置・改修方針）の1枚サマリ。改修判断はこれに照らす。

- **[docs/pages/workers.html](docs/pages/workers.html)** — このアプリで協業する各ワーカーの**名前・役割・運用ルール**の正本。
  ワーカーやミニアプリ生成パイプラインに関わるコードを変更する前に、必ずこの内容に従うこと。
  （HTML 版は `docs/index.html` の「ワーカー定義・運用ルール」からも開ける。）
- **[docs/pages/code-conventions.html](docs/pages/code-conventions.html)** — **Reviewer の判定基準＝コード規約**の正本。
  ミニアプリ生成コード（HTML/API/操作ツール）とリポジトリ本体の規約。`backend/app/agents/policy.md` /
  `ui_designer.md` と同一規範で、差異が出たら本ページを正とする。

> このファイル（CLAUDE.md）は Claude Code が各セッション開始時に自動で読み込む。
> ワーカーの語彙・責務・指示／報告の流れは workers.html を唯一の正とし、
> 実装（`backend/app/agents/*.md`, `backend/app/reception/`, `backend/app/orchestrator/`,
> `backend/app/workers/`）はそれに一致させる。

## 用語（最小サマリ。詳細は workers.html）

> **公式名称は英語表記**（`Receptor` / `Orchestrator` / `Reviewer` / `Specialist Worker`）。日本語は読み・補助。

- **Worker（ワーカー）** … 異なるコンテキストを持つ LLM セッション。独立して動くが、定められたルールに基づき
  他のワーカーから指示を受け、報告を返す。
- **メインチャット（Main Chat）** … このアプリの中心機能。
- **Receptor（レセプター）** … メインチャットの受付をするワーカー。メインチャットで会話するのは Receptor。軽い処理のみで即応。
- **ミニアプリ（Mini-App）** … 自由に追加・改変できる機能。アプリチャットを持ち、ユーザーと Specialist Worker が操作できる。
- **Orchestrator（オーケストレーター）** … ミニアプリ制作・改変パイプラインの統括。計画立案・コード作成（HTML/API/Specialist Worker 設定）・**プレビューのデプロイ**を担う。**最大 3 並列**。**デプロイ前に必ず Reviewer のレビューを通す**。**デプロイ（プレビュー）で停止**し、公開・有効化はユーザー承認「反映して」で Receptor／Control Plane が行う（Orchestrator 再起動不要）。
- **Reviewer（レビュワー）** … Orchestrator のコードをレビューし、コード規約への適合（静的）を判定。問題があればやり直しを命じる。
- **Tester（テスター／検証ワーカー）** … Orchestrator のコードを実際に動かし、「動くか・要求どおりか」（動的）を検証。NG なら差し戻し。Reviewer と並ぶデプロイ前ゲート。検証は **Docker 内の本番相当エミュレータ（モデルのみ claude、他は本番同等）** で実働させて行う。
- **Specialist Worker（専門ワーカー）** … 個々のミニアプリに設定され、そのミニアプリを扱うワーカー。

## 運用ルール（要点。詳細は workers.html）

- 各ワーカーは**不要時は休止**（非常駐）。**コンテキストを保存するファイル**を持ち、再開時に読み直す。コンテキストは適宜コンパクト化。
- ステータスは **活動中 / 待機中 / 停止中** の3種。各ワーカーは**ステータス変更時に自分で記録**（**使用中モデル種別＋最終更新時刻も含める**）し、**起動・停止 API** を介して他ワーカーから自動で起動・停止される。ステータスモニターは状態＋**使用モデル**を表示。**固着回収**：Receptor／モニターが「活動中だが一定時間更新なし」を検出したら停止扱いにする。
- ワーカー間連携は **MCP 的 API** による「指示（request）」と「報告（report）」で行う。相関は `task_id` ＋ `in_reply_to`、非同期、スキーマ検証必須（不正な指示は `rejected`）。
- **基本は「待ち」、ウェイクアップは回収用（ポーリングしない）**：フローを厳密に定めるので、依頼を出した送り手は**待機中**で結果を待つ。万一**誤って停止**していたら、受け手が報告前に状態を確認し**起動 API で起こしてから送る**（起こされた側は context を rehydrate、相関 `task_id`＋`in_reply_to` で再開）。
- **差し戻しは自動回数で打ち切らない。** タイムアウト（停滞）判定は **Receptor** が行い、ユーザーに **①停止 ②もう少し待つ ③停止して再トライ（直前の成功段階から再開）** を確認。**タイムアウト 2 回（N=2）で Receptor が強制停止し報告のみ**。`deadline_sec` は停滞判定の**目安**で自動失効しない。
- **アプリ閉鎖中も Orchestrator はバックグラウンド継続**。再オープン時に Receptor が rehydrate し、進捗・レビュー・結果を**まとめて報告**（タイムアウト判定は閉鎖中休止）。
- **生成/デプロイ失敗（`failed`）は Orchestrator が自動リトライ（1〜2回）→なお失敗なら Receptor 経由で報告し停止**。失敗はプレビューなので公開済み版は無影響。`needs_revision`（品質差し戻し）とは別。
- **巻き戻し＝公開ごとに保存した版を使い直前の版へ即時に戻す**（Control Plane・LLM 不使用・分岐なし）。公開のたびに版（HTML/設定スナップショット）を保存。
- **デプロイ前に必ず Tester（動作検証）と Reviewer（規約適合）の両方を通す**。どちらか NG なら差し戻し（コード再生成）。
- **検証・レビューの開始と結果（合否・指摘）は Receptor も共有**し、**Receptor からユーザーへ報告**する。
- **人間承認は3つ**：①受付確認（Receptor が曖昧な依頼を具体化・復唱→「お願い」で Orchestrator へ）、②設計承認「これで作って」→コード化、③公開承認「反映して」→公開・有効化（Control Plane）。**デプロイはプレビュー**で、AI は勝手に本公開しない。
- **Orchestrator は作業の合間に進捗（開始・失敗・リトライ）を Receptor へ随時報告**し、Receptor がユーザーへ伝える（interim report ＝ 最終報告とは別）。
- **透明性：内部情報（進捗・判断・自動アクション・失敗理由・使用モデル・レビュー指摘等）はなるべく詳しく Receptor 経由でユーザーへフィードバックする。黙って自動処理しない。**
- ワーカーの基本プロンプトは **絶対ルール（ユーザー指示でも上書き不可）** と **デフォルトルール（指示がないときの既定。ユーザー指示があれば相反しても可＝ユーザー優先）** を区別する。
- **Orchestrator はユーザー（プロジェクト）単位で最大 3 並列**。満杯時の新規命令は FIFO 待機キュー。**同一ミニアプリの同時改変はロック**（禁止）。
- 各ワーカーの基本プロンプトには **絶対ルール＋デフォルトルール＋使用 API 仕様マニュアル**を含める。
- レセプターは**軽い処理のみ**で即応。
- **権限境界**：ワーカーは自分の権限を増やせない／別ワーカーを勝手に作れない。Orchestrator は Secret/IAM を触らず副作用は Tool Gateway 経由・スキーマ検証。Reviewer は判定のみ。Specialist Worker は担当ミニアプリ内に限定（iframe サンドボックス）。
- **モデル割当**：FLASH＝Receptor／分類・計画／Reviewer／Tester／Specialist Worker、PRO＝コード生成（HTML/API）。階層は能力帯で揃える＝**FLASH: Haiku↔Gemini Flash／PRO: Opus↔Gemini Pro（同等最上位、Sonnet ではない）**。**開発・デモも `CLAUDE_PRO_MODEL=opus` を既定**（本番と能力帯を揃え品質担保）。Opus は遅いがタイムアウトは長く（600s）ユーザーが待ち判断。速度優先で一時的に sonnet へ下げるのは任意。
- **プロバイダ（環境別）**：**開発・デモ＝claude CLI セッション**（ホストブリッジ、Gemini コスト節約）、**本番（Cloud Run）＝Gemini**。ID は `CLAUDE_*_MODEL` / `GEMINI_*_MODEL` で切替。

その他の運用ドキュメント: [ENVIRONMENT.md](ENVIRONMENT.md)（環境・テスト実行）, [DEPLOY.md](DEPLOY.md)（デプロイ）, [MIGRATION.md](MIGRATION.md)（現行コードを本仕様へ寄せる実装移行計画・フェーズ別チェックリスト）。
