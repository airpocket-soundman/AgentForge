# AgentForge backend (Cloud Run, Python 3.12)
# Install into the project venv:  backend/.venv
# pip install -r requirements.txt

# Web framework
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-dotenv>=1.0
httpx>=0.27  # claude-cli provider -> host bridge (also pulled by google-genai)

# Google Cloud
google-cloud-firestore>=2.19
google-cloud-storage>=2.18
google-cloud-tasks>=2.16

# Gemini / Agents (added when implementing the orchestrator/workers)
google-genai>=1.0
# google-adk>=0.1   # uncomment in Phase 2 when wiring ADK
